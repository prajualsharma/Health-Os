## Shared secrets (override in your own .env)
# Generate a strong 32+ char secret for JWT HMAC
JWT_SECRET=change-me-to-a-long-random-secret

## Google OAuth (optional)
# Used by api-gateway to validate Google ID token aud.
GOOGLE_CLIENT_ID=

