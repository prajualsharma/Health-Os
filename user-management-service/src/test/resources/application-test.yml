spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/healthos
    username: healthos
    password: healthos
  data:
    redis:
      host: localhost
      port: 6379

healthos:
  security:
    jwt:
      secret: test-jwt-secret-at-least-32-characters-long
  otp:
    dev-bypass: true
    dev-code: "123456"
  notification:
    enabled: false
